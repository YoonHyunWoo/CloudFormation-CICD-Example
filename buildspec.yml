version: 0.2

phases:
  pre_build:
    commands:
        - aws configure set default.region ap-northeast-2
        - aws delete-stack --stack-name yhwStack

  build:
    commands:
      - create-stack --template-body $(cat CloudFormationTest.yaml) --stack-name yhwStack
      
